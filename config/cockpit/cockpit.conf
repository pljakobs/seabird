[WebService]
# Tell Cockpit its web root is at /cockpit so it generates correct asset
# URLs and redirects when served through the Caddy sub-path proxy
# (handle_path /cockpit* { reverse_proxy 127.0.0.1:9090 }).
UrlRoot = /cockpit
