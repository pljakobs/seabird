# bootc-image-builder configuration for CM4 / arm64
#
# This mirrors the standalone seabird layout and keeps disk-specific tuning
# outside the image definition.

[customizations.kernel]
append = "quiet systemd.unified_cgroup_hierarchy=1 cma=256M"
